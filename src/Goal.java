/*
 * To change this license header, choose License Headers in Project Properties.
 * To change this template file, choose Tools | Templates
 * and open the template in the editor.
 */

/**
 *
 * @author madsilva
 */
public class Goal extends Tray {
    // Each Goal has a tray array
    // how to compare goal tray to game tray?
    // if goal tray matches up to game tray, then proceed to next step
    // go thru all blocks in goal and check if they exist in the game tray
    // if they all do then check returns true
    
    public Goal() {
        super(0, 0);
    }
    
    // need to add goal instance variable to tray
    
    public boolean check(Tray game) {
        
    }
}
